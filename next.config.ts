import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['positive-life-875d223e2a.media.strapiapp.com', 'localhost', 'nathan.binarybridges.ca'],
  },
};

export default nextConfig;
